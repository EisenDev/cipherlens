# Developer Utility Scripts

This folder contains utility scripts to automate development tasks.

## Scripts

### `bootstrap.sh`
Performs system dependency validation, sets up a local Python virtual environment inside `scanner/`, installs Python requirements, and boots the PostgreSQL and Redis containers.

To run:
```bash
./scripts/bootstrap.sh
```
